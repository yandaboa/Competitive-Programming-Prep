#include <iostream>
#include string
using namespace std;

int main(){
    string basis;
    int numQs;
    cin >> basis;
    basis.insert(0, ".");

    
    cin >> numQs;
    for(int i = 0; i < numQs; i++){
        int start, end;
        cin >> start >> end;
    }
    return 0;
}
