import java.util.*;

public class convintervals{
    public static void main(String[] args){
        Scanner input = new Scanner (System.in);
        int numIntervals = input.nextInt();
        int m = input.nextInt();
        HashMap<Integer, Integer> intervals = new HashMap<Integer, Integer>();
        for(int i = 0; i < numIntervals; i++){
            intervals.put(input.nextInt(), input.nextInt());
        }
        for(int k = 0; k < 2*m; k++){
            int total = 0;
            for(int j = k; j < )
        }
    }
}